---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'div'>;

const { class: className, ...attrs } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-content px-4 sm:px-8 lg:px-12', className]} {...attrs}>
  <slot />
</div>
